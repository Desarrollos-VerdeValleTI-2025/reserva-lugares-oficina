# Brechas de Información — Reserva de Lugares de Oficina

Resultado de la Fase 4 (validación entre contexto funcional proporcionado por el usuario, evidencia en código, y lo registrado en `knowledge/`). Estado inicial de todas las entradas: **Pendiente**.

**Actualización 2026-09-10 (primera pasada):** el desarrollador responsable (practicantedes@verdevalle.com) respondió la mayoría de los huecos de impacto Alto y Medio. Los estados se actualizaron a **Resuelto** o **N/A** según corresponda. **No se borra ninguna fila** — se conserva el hueco original y se agrega la columna "Nota de resolución" con la respuesta textual del desarrollador, para dejar trazabilidad de qué se preguntó y qué se contestó.

**Actualización 2026-09-10 (segunda pasada):** el desarrollador respondió los 4 huecos restantes (GAP-11, GAP-13, GAP-14, GAP-15). Con esta pasada, **ya no queda ningún hueco en estado "Pendiente"** en esta tabla: todos quedan en **Resuelto** o **N/A**.

## Impacto Alto

| ID | Información faltante | Por qué importa | Sección afectada | Acción sugerida | Fuente revisada | Estado | Nota de resolución |
|---|---|---|---|---|---|---|---|
| GAP-01 | No existe pipeline de CI/CD en el repositorio. | Sin CI/CD, los despliegues dependen de pasos manuales no documentados, con riesgo de error humano y falta de trazabilidad. | 7.3 CI/CD | Confirmar con el equipo si el despliegue es manual; si lo es, documentarlo formalmente; si se planea automatizar, definir la herramienta. | Búsqueda de `.github/workflows`, `azure-pipelines.yml`, `.gitlab-ci.yml`, `Jenkinsfile` en todo el repositorio (sin resultados) | **Resuelto** | Confirmado por el desarrollador (2026-09-10): el despliegue es **manual**. No hay CI/CD y no está planeado por ahora. Es un hecho confirmado de alcance del proyecto, no un pendiente de investigación. |
| GAP-02 | No existen pruebas automatizadas (unitarias, integración, E2E) en frontend ni backend. | Alto riesgo de regresiones al modificar lógica de reservas/disponibilidad, especialmente en los stored procedures y en el middleware de autenticación. | 8.1 Estrategia de Pruebas | Priorizar pruebas de integración sobre los endpoints de reservas (crear/editar/cancelar) y unitarias sobre `authMiddleware.js` y los servicios. | `package.json` de front y api (sin dependencias de testing) | **N/A** | Confirmado por el desarrollador: no se realizaron pruebas automatizadas por tratarse de una aplicación de capacitación. No aplica para el alcance actual del proyecto (no es un pendiente por implementar). |
| GAP-03 | No hay procedimiento formal de despliegue a QAS/Producción documentado, más allá de la URL `practicadespliegue.verdevalle.com.mx` presente en `.env.qas`. | Sin este procedimiento, no se puede reproducir un despliegue de forma confiable ni auditar cómo se publicó lo que corre en QAS. | 7.2 Procedimiento de Despliegue | Documentar cómo se construye y publica el build actual. | README raíz (solo documenta ejecución local), ausencia de Docker/CI/CD | **Resuelto** | Confirmado por el desarrollador: el despliegue se realiza por medio de **Nginx**, que sirve/proxya la aplicación. Documentado en `DEPLOYMENT.md` y en la sección 7.2 del documento técnico. |
| GAP-04 | Contraseña de base de datos hardcodeada en texto plano en un script SQL **versionado en git** (`01_create_database.sql`), coincidente con la contraseña real de `.env.qas`. | Riesgo de seguridad crítico: credencial real expuesta en el historial de un repositorio (aunque sea privado/de capacitación). | 6.2 Cifrado y Datos Sensibles / 4.3 Reglas de Integridad | Rotar la contraseña de inmediato; remover el valor hardcodeado del script; evaluar purgar el historial de git si aplica. | `reserva-lugares-oficina-db/scripts/01_create_database.sql`, `reserva-lugares-oficina-api/.env.qas` | **Resuelto** | Confirmado por el desarrollador: la contraseña ya fue **rotada/cambiada** por el equipo. El valor anterior (`Pr@cticanteDes04`) permanece en el historial de git, pero ya no es una credencial válida. Se mantiene como recomendación general no volver a hardcodear secretos en scripts versionados. |
| GAP-05 | Contradicción entre el README de `reserva-lugares-oficina-db` (usuario de aplicación con **solo** permiso `EXECUTE`, vía rol) y el script real `01_create_database.sql` (otorga `SELECT, INSERT, UPDATE, EXECUTE` directo). | El principio de mínimo privilegio documentado no se cumple en la implementación real; genera confusión sobre cuál es la configuración "correcta" del proyecto. | 4.3 Reglas de Integridad | Decidir y unificar: o se corrige el script para usar el enfoque de rol con solo `EXECUTE`, o se actualiza el README para reflejar la realidad. | `reserva-lugares-oficina-db/README.md` vs. `01_create_database.sql` | **Resuelto** | Aclarado por el desarrollador: los permisos amplios del script (`SELECT, INSERT, UPDATE, EXECUTE`) fueron **intencionales**, para crear el usuario de aplicación dentro de la base de datos durante el despliegue; ya se ejecutó así en el entorno real. No es una contradicción sin resolver, es el diseño confirmado. Queda fuera del alcance de este agente actualizar el README de la BD para reflejar esta realidad, pero se deja anotado como sugerencia menor. |

## Impacto Medio

| ID | Información faltante | Por qué importa | Sección afectada | Acción sugerida | Fuente revisada | Estado | Nota de resolución |
|---|---|---|---|---|---|---|---|
| GAP-06 | No se encontró Docker ni definición de infraestructura como código. | Dificulta reproducibilidad de ambientes y portabilidad del despliegue. | 7.3 CI/CD / 2.4 Diagramas | Evaluar si conviene contenerizar el backend. | Búsqueda de `Dockerfile`/`docker-compose.yml` en todo el repositorio (sin resultados) | **Resuelto** | Mismo caso que GAP-01: el desarrollador confirmó que el despliegue es manual (vía Nginx) y que no está planeado usar Docker por ahora. |
| GAP-07 | No se encontró estrategia de backup/retención para MySQL. | Sin backup documentado, una falla de la base de datos implicaría pérdida total de reservas históricas. | 4.4 Backup y Mantenimiento | Documentar (o implementar) una política de backup, aunque el alcance actual sea de capacitación. | `reserva-lugares-oficina-db/` (sin scripts ni mención de backup) | **N/A** | Confirmado por el desarrollador: no existe backup ni mantenimiento de la base de datos por el momento. No aplica para el alcance actual (no es un pendiente por investigar, es una confirmación explícita). |
| GAP-08 | No se encontró monitoreo/observabilidad más allá de logs locales de Winston/Morgan (sin métricas, alertas, ni gestión de incidencias). | Sin monitoreo, un fallo en producción/QAS podría pasar desapercibido hasta que un usuario lo reporte. | 7.4 Monitoreo y Observabilidad | Evaluar una herramienta ligera de monitoreo/alertas si el proyecto avanza más allá de capacitación. | `src/config/logger.js`, ausencia de integraciones de APM | **N/A** | Confirmado por el desarrollador: el proyecto no avanza por ahora más allá de una actividad de capacitación, por lo que métricas, alertas y gestión de incidencias no aplican en esta etapa. |
| GAP-09 | No hay roles/permisos granulares (ej. administrador de espacios vs. empleado regular); el contexto funcional tampoco los menciona. | Podría limitar el crecimiento funcional del sistema (ej. gestión de catálogo de lugares, reportes). | 6.1 Autenticación y Autorización | Confirmar con negocio si se requieren roles diferenciados a futuro. | `authMiddleware.js`, controllers (sin verificación de rol) | **N/A** | Confirmado por el desarrollador: no es necesario especificar roles/permisos granulares por tratarse de una actividad de práctica. Es una decisión de alcance del proyecto, no un pendiente de negocio sin resolver. |
| GAP-10 | Posible inconsistencia entre el comportamiento implícito del frontend (trata `reservations[0]` como "la" reserva única del usuario) y el backend/BD, que no impiden que un usuario tenga varias reservas activas en fechas distintas simultáneamente. | Ambigüedad funcional: no está claro si "una reserva por usuario" es una regla de negocio real que falta implementar, o si el sistema debería soportar múltiples reservas y la UI actual está incompleta. | 3.1 Requisitos Funcionales / Módulo de reservas | Confirmar con negocio/usuario cuál es la regla correcta. | `Inicio.jsx` (`reservations[0]`), `web_se_reservations_byuser.sql`, `web_in_create_reservation.sql` | **Resuelto** | Aclarado por el desarrollador: el sistema **no** limita al usuario a una sola reserva activa — puede tener más de una reserva en fechas distintas. Esa restricción de "una sola reserva" simplemente **no está implementada todavía** en esta versión. No es un bug ni una ambigüedad: es el comportamiento actual confirmado. La UI que solo muestra `reservations[0]` queda documentada como limitación conocida de la interfaz, no como una regla de negocio. |
| GAP-11 | `console.log` de depuración en `authMiddleware.js` no condicionado por ambiente. | Podría filtrar detalles de errores de verificación JWT en logs de QAS/Producción. | 6.1 Autenticación y Autorización | Remover o condicionar el `console.log` a `NODE_ENV==='development'`. | `src/middlewares/authMiddleware.js` | **Resuelto** | **Corregido en código** por el equipo (confirmado por lectura directa de `reserva-lugares-oficina-api/src/middlewares/authMiddleware.js` el 2026-09-10): el `console.log('DEBUG jwt.verify error ->', ...)` fue reemplazado por `logger.debug('JWT verification failed', { errorName: err.name, errorMessage: err.message })`, usando el logger de `winston` ya existente en `src/config/logger.js` (mismo patrón que `errorHandler.js`). El nivel del logger es `'debug'` en desarrollo e `'info'` en calidad/producción, por lo que en QAS/Producción ese log ya no se emite. Este agente no modificó código; solo se leyó el archivo para constatar el cambio ya aplicado. |
| GAP-12 | No se encontró evidencia de gestión de vulnerabilidades de dependencias (Dependabot, `npm audit` en pipeline, SAST/DAST). | Sin este proceso, dependencias con vulnerabilidades conocidas podrían pasar desapercibidas. | 6.4 Gestión de Vulnerabilidades | Habilitar Dependabot/Renovate y/o agregar `npm audit` a un futuro pipeline. | Ausencia de `.github/` en el repositorio | **N/A** | Confirmado por el desarrollador: por ser una actividad de capacitación no se van a agregar herramientas de gestión de vulnerabilidades de dependencias por ahora. |

## Impacto Bajo

| ID | Información faltante | Por qué importa | Sección afectada | Acción sugerida | Fuente revisada | Estado | Nota de resolución |
|---|---|---|---|---|---|---|---|
| GAP-13 | No se determinó el ambiente de Producción real (no existe `.env.prd`, aunque `.gitignore` lo contempla). | Indica que producción aún no está configurada, o que su configuración vive fuera de este repositorio. | 7.1 Entornos | Confirmar si existe (o se planea) un ambiente de producción real y documentarlo. | `.gitignore` de front y api (mencionan `.env.prd`, archivo inexistente) | **N/A (confirmado)** | Confirmado por el desarrollador (2026-09-10): el proyecto es, por ahora, únicamente una actividad de capacitación/QAS. **No existe ni está planeado** un ambiente de Producción real por el momento. Deja de ser un pendiente de investigación y pasa a ser un N/A explícito de alcance. |
| GAP-14 | Archivo vacío `practicante-desarrollo`, versionado en la raíz de `reserva-lugares-oficina-front/`, con el mismo nombre que la rama de git activa. | Ruido en el repositorio; posible artefacto accidental. | Anexo — limpieza de repositorio | Eliminar el archivo del repositorio. | `git ls-files`, contenido confirmado vacío | **Resuelto** | Confirmado el 2026-09-10: el archivo **ya no existe** en el working tree de `reserva-lugares-oficina-front/` (verificado por listado de directorio). El desarrollador lo eliminó del repositorio. |
| GAP-15 | No se encontró documentación Swagger/OpenAPI de la API. | Dificulta la exploración interactiva de la API para nuevos desarrolladores o QA. | 5.1 Endpoints | Evaluar agregar `swagger-jsdoc`/`swagger-ui-express` si el proyecto crece. | Ausencia de `swagger.json`/`openapi.yaml` en el repositorio | **N/A (mejora futura, no crítica)** | El desarrollador decidió (2026-09-10) **no implementarla por ahora**. Queda anotada únicamente como mejora futura a evaluar si el proyecto crece más allá de la actividad de capacitación actual. No es un pendiente crítico ni requiere "⚠ Requiere revisión". |

## Funcionalidades mencionadas por el usuario y su verificación en código

| Funcionalidad mencionada por el usuario | ¿Verificada en código? |
|---|---|
| Login con cuenta corporativa (Azure AD) | Sí — `msalConfig.js`, `authMiddleware.js` |
| Consulta de disponibilidad de asientos por fecha | Sí — `GET /api/seats`, `web_se_seats_bydate` |
| Creación de reserva | Sí — `POST /api/reservations`, `web_in_create_reservation` |
| Edición de reserva | Sí — `PUT /api/reservations/:id`, `web_up_update_reservation` |
| Cancelación de reserva (lógica, nunca DELETE físico) | Sí — `DELETE /api/reservations/:id`, `web_de_cancel_reservation` |
| Ver reservas activas | Sí — `GET /api/reservations/me`, `web_se_reservations_byuser` (la UI solo muestra la primera; confirmado que el sistema sí permite varias — ver GAP-10, resuelto) |
| Contrato de datos fijo desde las primeras iteraciones | Sí — confirmado en código y en los 3 README del repositorio |
| Dos App Registrations distintos (front / API con scope `API.Access`) | Sí — `msalConfig.js`, `authMiddleware.js`, README de front y api |
| API consume MySQL real vía `mysql2/promise`, siempre por stored procedures | Sí — `seatsService.js`, `reservationsService.js` |

No se identificaron funcionalidades mencionadas por el usuario que estén ausentes en el código, ni funcionalidades relevantes encontradas en código que el usuario no haya mencionado (más allá del detalle de implementación ya cubierto en `BUSINESS_RULES.md` y `ARCHITECTURE.md`).

## Resumen de estado (actualización 2026-09-10, segunda pasada — cierre de huecos)

| Estado | Cantidad | IDs |
|---|---|---|
| Resuelto | 8 | GAP-01, GAP-03, GAP-04, GAP-05, GAP-06, GAP-10, GAP-11, GAP-14 |
| N/A (confirmado, no aplica al alcance actual / mejora futura no crítica) | 7 | GAP-02, GAP-07, GAP-08, GAP-09, GAP-12, GAP-13, GAP-15 |
| Pendiente | 0 | — |

**No quedan huecos en estado "Pendiente".** Los 15 gaps identificados en la validación original (Fase 4) están todos en Resuelto o N/A confirmado. Esto significa que el documento HTML de revisión (`documento-tecnico-revision.html`) ya no debería contener bloques "⚠ Requiere revisión" derivados de decisiones pendientes del desarrollador — solo quedaría, en su caso, la leyenda general del documento como borrador hasta aprobación formal.
