# Riesgos y Deuda Técnica — Reserva de Lugares de Oficina

> **Actualización 2026-09-10 (segunda pasada):** el desarrollador respondió el resto de los huecos que originaban estos riesgos (incluye R5 y R14). Se conserva cada riesgo (no se elimina ninguno), y se anota su estado real: **Mitigado/Resuelto** (ya no requiere acción) o **Aceptado (decisión de alcance)** (el equipo decidió conscientemente no atenderlo por ahora, dado que es un proyecto de capacitación). Los únicos riesgos que permanecen sin acción tomada (no confirmados como aceptados ni corregidos) son R8, R9 y R11, de impacto medio/bajo, sin marcador crítico.
>
> **Actualización 2026-09-10 (tercera pasada — cierre):** se corrigieron en código los 2 últimos riesgos abiertos sin decisión (R8 y R9). Verificado por lectura directa (sin modificar código): (1) R8 — se agregó `express-rate-limit` (`apiLimiter`) en `reserva-lugares-oficina-api/src/app.js`, aplicado sobre `/api` (300 solicitudes / 15 min por IP), montado **antes** de `authMiddleware`; la dependencia `express-rate-limit ^8.7.0` está en `package.json`. (2) R9 — `reserva-lugares-oficina-api/src/config/logger.js` ahora usa `winston-daily-rotate-file` (`DailyRotateFile`) para `logs/error-%DATE%.log` y `logs/combined-%DATE%.log`, con `maxSize: '20m'` y `maxFiles: '14d'`; la dependencia `winston-daily-rotate-file ^5.0.0` está en `package.json`. Con esto, de los riesgos de impacto Medio, solo **R11** permanece en estado **Pendiente** (sin acción ni decisión tomada), pero es de impacto Medio y sin marcador crítico. No quedan riesgos de impacto Alto en estado Pendiente.
>
> **Actualización 2026-09-10 (cuarta pasada — cierre de ciclo de documentación):** el desarrollador corrió `npm outdated` y aplicó `npm update` en front y api, actualizando todo lo disponible **dentro de las mismas versiones mayores** ya en uso (sin saltar de mayor). Verificado por lectura directa de `package.json` y de las versiones resueltas en `package-lock.json` de ambos proyectos: front → `react`/`react-dom` `19.3.0`, `react-router-dom` `7.18.3`, `antd` `6.6.3`, `vite` `8.3.0`, `eslint` `10.10.0`; api → `mysql2` `3.24.4`, `express` `5.2.1` (sin cambio de versión resuelta, ya estaba en esa versión). El desarrollador reportó `/health` → 200 tras el cambio en la API y `npm run build` exitoso en el front; esas dos verificaciones no se repitieron en vivo dentro de esta sesión (se tomó la palabra del desarrollador), pero sí se corrió `npm run lint` en el front directamente en esta sesión, confirmando 0 errores. **R11 pasa a Mitigado** (se cerró el desfase de parches/minor dentro de la misma mayor; el riesgo de fondo — estar en versiones mayores recientes — se mantiene como aceptado, no como pendiente). **R15 pasa a Resuelto** (`reserva-lugares-oficina-front/package.json` ya tiene `"version": "1.0.0"`, igual que la API). Adicionalmente, se registra un **nuevo riesgo R16** (Bajo-Medio, ya Resuelto): un error real de ESLint (`react-hooks/set-state-in-effect`) en `src/pages/Inicio.jsx`, preexistente y no causado por esta actualización de dependencias, ya corregido en código (verificado por lectura directa) y con `npm run lint` limpio. **Con esta pasada, no queda ningún riesgo de impacto Alto ni Medio en estado "Pendiente"; tampoco de impacto Bajo.** Todos los riesgos de esta tabla quedan en estado Mitigado, Resuelto o Aceptado (decisión de alcance).

## Impacto Alto

| ID | Riesgo | Probabilidad | Impacto | Mitigación sugerida | Fuente | Estado (2026-09-10) |
|---|---|---|---|---|---|---|
| R1 | Contraseña de base de datos en texto plano, commiteada en `reserva-lugares-oficina-db/scripts/01_create_database.sql`, coincidente con la contraseña real usada en `.env.qas` local. | Alta (ya expuesta en el historial de git) | Alto (histórico) | Rotar la contraseña; remover el valor hardcodeado del script; evitar volver a hardcodear secretos en scripts versionados. | Código (`01_create_database.sql`) | **Mitigado.** La contraseña ya fue rotada por el equipo. El valor anterior permanece en el historial de git pero ya no es válido; se mantiene la recomendación de no repetir la práctica. |
| R2 | El script `01_create_database.sql` otorga `SELECT, INSERT, UPDATE, EXECUTE` directo al usuario de aplicación, en apariencia distinto al README de la BD (que documenta un usuario con solo `EXECUTE` vía rol dedicado). | Alta (ya aplicado) | Medio (reclasificado) | Ninguna acción urgente; opcionalmente, actualizar el README de la BD para reflejar el diseño real. | Código (`01_create_database.sql` vs `reserva-lugares-oficina-db/README.md`) | **Resuelto — no era una contradicción.** El desarrollador confirmó que los permisos amplios fueron intencionales, para crear el usuario de aplicación durante el despliegue; ya se ejecutó así. |
| R3 | No se encontró ninguna prueba automatizada (unitaria, integración, E2E) en frontend, backend ni base de datos. | Alta (confirmado) | Alto (para un sistema real; bajo para el alcance actual) | Incorporar pruebas si el proyecto avanza más allá de capacitación. | `DEPENDENCIES.md` (sin librerías de testing) | **Aceptado (decisión de alcance).** Confirmado por el desarrollador: no se hicieron pruebas por tratarse de una aplicación de capacitación. Se mantiene registrado como riesgo latente si el proyecto creciera. |
| R4 | No se encontró CI/CD, ni procedimiento formal de despliegue a QAS/Producción, ni Docker. | Alta | Alto (para operación a escala; bajo para el alcance actual) | Ninguna acción urgente al alcance actual; reevaluar si el proyecto pasa a producción real. | `DEPLOYMENT.md` | **Aceptado (decisión de alcance).** Confirmado por el desarrollador: el despliegue es manual, vía Nginx; no hay CI/CD ni Docker planeado por ahora. |

## Impacto Medio

| ID | Riesgo | Probabilidad | Impacto | Mitigación sugerida | Fuente | Estado (2026-09-10) |
|---|---|---|---|---|---|---|
| R5 | `authMiddleware.js` tenía un `console.log('DEBUG jwt.verify error ->', err.name, '-', err.message)` no condicionado por `NODE_ENV`, que se ejecutaba en cualquier ambiente (incluida potencialmente producción) y quedaba fuera del pipeline de `winston`. | Media (histórico) | Bajo (mitigado) | ~~Remover el `console.log` o reemplazarlo por `logger.debug(...)` condicionado a `NODE_ENV==='development'`.~~ Ya aplicado. | Código (`authMiddleware.js`) | **Mitigado (2026-09-10).** Corregido en código: se reemplazó por `logger.debug('JWT verification failed', { errorName: err.name, errorMessage: err.message })`, usando el logger `winston` existente (nivel `debug` en desarrollo, `info` en calidad/producción — ya no se emite fuera de desarrollo). Confirmado por lectura directa del archivo. Ver `INFORMATION_GAPS.md` (GAP-11, Resuelto). |
| R6 | No existe restricción (constraint ni validación en SP) que limite a una reserva activa por usuario; el frontend solo muestra la primera (`reservations[0]`), pero el backend permite varias simultáneas en fechas distintas. | Media | Bajo (reclasificado) | Ninguna acción requerida por ahora; si negocio decide limitar a una reserva por usuario en el futuro, implementar la validación. | Código (`web_in_create_reservation.sql`, `Inicio.jsx`) — ver `INFORMATION_GAPS.md` GAP-10 | **Resuelto — comportamiento confirmado.** El desarrollador aclaró que el sistema no limita al usuario a una sola reserva activa; es el comportamiento actual válido, no una omisión. La UI que solo muestra la primera reserva queda como limitación conocida de interfaz (ver `FRONTEND.md`). |
| R7 | No hay roles/permisos granulares: cualquier cuenta corporativa autenticada tiene acceso total a todas las operaciones de reserva. | Media | Medio (para crecimiento futuro; bajo para el alcance actual) | Evaluar roles si el alcance funcional crece (ej. administrador de espacios). | Código (`authMiddleware.js`, controllers) | **Aceptado (decisión de alcance).** Confirmado por el desarrollador: no es necesario especificar roles/permisos por ser una actividad de práctica. |
| R8 | No se encontró `express-rate-limit` ni ningún límite de tasa de peticiones en el backend. | Media (histórico) | Bajo (mitigado) | ~~Agregar un límite general de requests, aunque el riesgo esté parcialmente mitigado por depender de Azure AD para autenticar.~~ Ya aplicado. | `DEPENDENCIES.md`, `SECURITY.md` | **Mitigado (2026-09-10).** Corregido en código: se agregó `express-rate-limit` (`^8.7.0` en `package.json`) y se configuró `apiLimiter` en `reserva-lugares-oficina-api/src/app.js` (`windowMs: 15 * 60 * 1000`, `max: 300` por IP), montado con `app.use('/api', apiLimiter)` **antes** de `app.use('/api', authMiddleware)`. Confirmado por lectura directa de `app.js` y `package.json`. |
| R9 | Logs de `winston` a archivo (`logs/error.log`, `logs/combined.log`) sin rotación ni límite de tamaño configurado. | Media (histórico) | Bajo (mitigado) | ~~Incorporar `winston-daily-rotate-file` o una política de rotación externa (logrotate).~~ Ya aplicado. | Código (`config/logger.js`) | **Mitigado (2026-09-10).** Corregido en código: `reserva-lugares-oficina-api/src/config/logger.js` ahora usa `winston-daily-rotate-file` (`^5.0.0` en `package.json`) mediante `DailyRotateFile` para `logs/error-%DATE%.log` y `logs/combined-%DATE%.log`, con `datePattern: 'YYYY-MM-DD'`, `maxSize: '20m'` y `maxFiles: '14d'`. Confirmado por lectura directa de `logger.js` y `package.json`. |
| R10 | No se encontró evidencia de gestión de vulnerabilidades de dependencias (Dependabot, `npm audit` en pipeline, SAST/DAST). | Media | Medio (para producción real; bajo para el alcance actual) | Habilitar Dependabot/Renovate y `npm audit` si el proyecto avanza más allá de capacitación. | `SECURITY.md` (ausencia confirmada de `.github/`) | **Aceptado (decisión de alcance).** Confirmado por el desarrollador: por ser capacitación, no se van a agregar estas herramientas por ahora. |
| R11 | Varias dependencias en versiones mayores muy recientes (React 19, Express 5, Vite 8, Ant Design 6, ESLint 10, react-router-dom 7) con menor tiempo de maduración del ecosistema. | Baja-Media | Medio | ~~Vigilar changelogs y breaking changes al actualizar; considerar fijar rangos de versión más conservadores si se detecta inestabilidad.~~ Ya aplicado: se corrió `npm outdated`/`npm update` en ambos proyectos. | `DEPENDENCIES.md` | **Mitigado (2026-09-10).** El desarrollador corrió `npm outdated` y `npm update` en front y api, actualizando todo lo disponible **dentro de las mismas versiones mayores** ya en uso (no se saltó de mayor — eso sigue siendo una decisión de alcance aceptada). Confirmado por lectura directa: front → `react`/`react-dom` `19.3.0`, `react-router-dom` `7.18.3`, `antd` `6.6.3`, `vite` `8.3.0`, `eslint` `10.10.0`; api → `mysql2` `3.24.4`. El desarrollador confirmó que la API sigue respondiendo (`/health` → 200) y que el front compila (`npm run build` exitoso) tras la actualización. **No se marca "Resuelto" al 100%** porque el riesgo de fondo (estar en versiones MAYORES recientes de React/Express/Vite) sigue existiendo por naturaleza; lo que se mitigó es el desfase de parches/minor dentro de esas mayores. |

## Impacto Bajo

| ID | Riesgo | Probabilidad | Impacto | Mitigación sugerida | Fuente | Estado (2026-09-10) |
|---|---|---|---|---|---|---|
| R12 | No se encontró estrategia de backup/retención documentada para MySQL. | No determinada | Bajo | Documentar backup/retención si el proyecto llegara a manejar datos reales. | `DATABASE.md` | **Aceptado (decisión de alcance).** Confirmado por el desarrollador: no existe backup ni mantenimiento por el momento; no aplica al alcance actual. |
| R13 | No se encontró monitoreo/observabilidad (métricas, alertas, APM) más allá de logs locales. | No determinada | Bajo | Evaluar una herramienta de APM/observabilidad si el proyecto pasa a producción real. | `DEPLOYMENT.md` | **Aceptado (decisión de alcance).** Confirmado por el desarrollador: el proyecto no avanza por ahora más allá de una actividad de capacitación; monitoreo formal no aplica en esta etapa. |
| R14 | Archivo vacío `practicante-desarrollo` versionado en la raíz de `reserva-lugares-oficina-front/`, con el mismo nombre que la rama de git activa — probable artefacto accidental. | Baja (histórico) | Bajo (mitigado) | ~~Eliminar el archivo del repositorio.~~ Ya aplicado. | Código (`git ls-files`, confirmado vacío y trackeado) | **Mitigado (2026-09-10).** El archivo ya no existe en el working tree (verificado por listado de directorio). Ver `INFORMATION_GAPS.md` (GAP-14, Resuelto). |
| R15 | `package.json` del frontend mantiene la versión por defecto de Vite (`0.0.0`), sin política de versionado semántico visible en ninguno de los dos componentes. | Baja | Bajo | ~~Adoptar SemVer si el proyecto necesita trazabilidad de versiones de release.~~ Ya aplicado. | `DEPENDENCIES.md`, `DEPLOYMENT.md` | **Resuelto (2026-09-10).** El desarrollador actualizó `"version": "0.0.0"` a `"version": "1.0.0"` en `reserva-lugares-oficina-front/package.json`, igualándola a la versión que ya tenía la API (`"version": "1.0.0"`). Confirmado por lectura directa de ambos `package.json`. |
| R16 | Error real de ESLint (regla `react-hooks/set-state-in-effect`) en `reserva-lugares-oficina-front/src/pages/Inicio.jsx`: la función `cargarReservas` (que actualiza estado vía `setReservations`/`setError`) se llamaba directamente dentro de un `useEffect`, lo cual puede causar renders en cascada. Riesgo preexistente, no detectado antes por falta de CI/CD que corra lint (ver GAP-01/R4). No fue causado por la actualización de dependencias de hoy (`eslint-plugin-react-hooks` se quedó en `^7.1.1`, sin cambio de versión). | Media (histórico, ya corregido) | Bajo-Medio (riesgo de performance/renders en cascada, no de seguridad) | ~~Mover la lógica async a una función autoejecutada dentro del propio `useEffect`.~~ Ya aplicado. | Código (`Inicio.jsx`), hallazgo nuevo del 2026-09-10 al correr `npm run lint` | **Resuelto (2026-09-10).** Corregido en código: se movió la lógica async a una IIFE dentro del `useEffect`, con una bandera `ignorar` para evitar actualizar estado si el componente se desmonta antes de que la respuesta llegue. `cargarReservas` se conservó aparte para el botón "Reintentar" y el flujo de cancelación (manejadores de evento, no efectos — ahí sí es válido llamarla directamente). Confirmado por lectura directa del archivo y por `npm run lint` sin errores en esta sesión. Ver `FRONTEND.md`. |
